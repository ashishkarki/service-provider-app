const NODE_ENV_OPTIONS = {
  DEV: 'development',
  PROD: 'production',
}

const EXPRESS_DEFAULT_PORT = 5001

const API_BASE_URI = '/api/v1/services'

const DATE_FORMAT = 'DD/MM/YYYY'

const NUM_TOTAL_SKILLS = 10
const NUM_SERVICE_REQUESTS = 5

module.exports = {
  NODE_ENV_OPTIONS,
  EXPRESS_DEFAULT_PORT,
  API_BASE_URI,
  DATE_FORMAT,
  NUM_TOTAL_SKILLS,
  NUM_SERVICE_REQUESTS,
}
